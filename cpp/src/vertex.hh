#ifndef VERTEX_HH
#define VERTEX_HH

#include <assert.h>

/** An vertex [src,dst] with label [lab] or part of it. */


template<typename I> /* Integer type, typically int, uint, or larger. */

class vertex {
private:
    I i;
public:
    typedef I 

} // vertex

#endif // VERTEX_HH
